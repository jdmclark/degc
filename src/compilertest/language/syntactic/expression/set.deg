module ca.ualberta

program Foo:
    assert {Type}
    assert {Type | false}
    assert {Type | true or (false or true)}
    assert {Type} union {Type}
    assert {Type} union {Type} union {Type}
    assert {Type} intersect {Type}
    assert {Type} setminus {Type}
    assert ({Type} union
            {Type} union
            {Type} setminus
            {Type})
