module ca.ualberta

program Foo:
    assert {}
    assert {Type}
    assert {Type | false}
    assert {Type | true or (false or true)}
    assert {} union {}
    assert {} union {} union {}
    assert {} intersect {}
    assert {} setminus {}
    assert ({} union
            {} union
            {} setminus
            {})
    assert universe
