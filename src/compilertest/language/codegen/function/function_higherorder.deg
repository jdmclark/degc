module ca.nullptr

function square(number a) -> number:
    a * a
    
function double(number a) -> number:
    2 * a
    
function map_sum(function(number)->number fn, number a, number b, number c) -> number:
    fn(a) + fn(b) + fn(c)
